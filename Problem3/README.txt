Problem: Find the Second Largest Item in a BST

Problem Definition:
Implement a function to find the second largest item in a binary search tree.

Solution: 
In SecondLargest.py, find_2nd_largest(root, size) takes the root and the size of a BST as input parameters and returns the second largest item in the BST in O(h) time and O(1) space.